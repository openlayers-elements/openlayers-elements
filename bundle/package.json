{
  "name": "@openlayers-elements/bundle",
  "version": "0.1.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "./build.js",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.16.4",
    "rimraf": "^3.0.2"
  }
}
